namespace next.core
{
    public enum CommonStatusTypes
    {
        Loading = 1,
        Ready = 10,
        Error = 20,
        Submitting = 30,
        SubmitFailed = 40,
        SubmitSucceeded = 50
    }
}
